{
  "name": "server",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "bun run --hot src/index.ts",
    "start": "bun run dist/index.js"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.4",
    "@project-base/auth": "workspace:*",
    "@project-base/db": "workspace:*",
    "bcryptjs": "^3.0.3",
    "better-auth": "catalog:",
    "dayjs": "^1.11.19",
    "dotenv": "catalog:",
    "hono": "^4.8.2",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/bcryptjs": "^3.0.0",
    "@types/bun": "^1.2.6",
    "tsdown": "catalog:",
    "typescript": "catalog:"
  }
}
